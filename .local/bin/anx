#!/usr/bin/env bash

if [ $# -eq 0 ]; then
    clear && figlet "ANX" | lolcat
    jrnl @should @annoying @will @need -not @done && doing today
    exit 0
fi

case "$1" in
    today|now|done|later|did|select|recent|undo)
        doing "$@"
        ;;
    *)
        jrnl "$@"
        ;;
esac
